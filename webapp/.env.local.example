# 複製這個檔案成 .env.local（同一個資料夾），再把下面兩個值換成你自己 Supabase 專案的。
# 去 Supabase 專案後台 -> Project Settings -> API 拿這兩個值。
# .env.local 已經被 .gitignore 排除，不會被提交。

NEXT_PUBLIC_SUPABASE_URL=https://your-project-ref.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-anon-public-key

# Google Cloud Console -> APIs & Services -> Credentials 拿這兩個值。
# 注意：GOOGLE_CLIENT_SECRET 沒有 NEXT_PUBLIC_ 前綴，只會在伺服器端讀到，
# 不會被打包進瀏覽器看得到的程式碼，這是刻意的，不要加上 NEXT_PUBLIC_ 前綴。
GOOGLE_CLIENT_ID=your-client-id.apps.googleusercontent.com
GOOGLE_CLIENT_SECRET=your-client-secret
